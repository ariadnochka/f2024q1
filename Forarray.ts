const arrayfruits = ['banana','apple','raspberry','strawberry','cherry']

for (let y = 0; y < arrayfruits.length; y++) {
    if (arrayfruits[y] === 'banana') {
        console.log(`в данном массиве найден $(arrayfruits[y]}`)
    }
}

   
